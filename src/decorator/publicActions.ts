import type { Chain, Client, Transport } from 'viem'

// PLACEHOLDER
export const publicActionSushi = <
  TChain extends Chain | undefined = Chain | undefined,
>(
  _client: Client<Transport, TChain>,
) => ({})
