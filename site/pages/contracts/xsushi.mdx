import { AddressTable } from '../../components/table'
import { XSUSHI_ADDRESS } from 'sushi/currency'

# xSushi Token

Currently xSushi is only available on mainnet, or bridged to Polygon through the [Polygon PoS bridge](https://portal.polygon.technology/bridge).

## Deployments

<AddressTable data={XSUSHI_ADDRESS} />