import { AddressTable } from '../../components/table'
import { LAUNCHPAD_V1_FACTORY_ADDRESSES } from '../../constants/launchpad'

# Launchpad

The Sushi Launchpad factory deploys fixed-supply tokens and creates their
one-sided SushiSwap V3 liquidity positions.

## V1 deployments

<AddressTable data={LAUNCHPAD_V1_FACTORY_ADDRESSES} />
