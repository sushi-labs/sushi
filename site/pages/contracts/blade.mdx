import { AddressTable } from '../../components/table'
import {
  BLADE_POOL_ADDRESSES,
  BLADE_COVE_ADDRESSES,
  BLADE_PERMIT_ROUTER_ADDRESSES,
  BLADE_LP_TRANSFER_ADDRESSES,
  BLADE_REGISTER_ADDRESSES,
  BLADE_FEE_SPLIT_ADDRESSES,
  BLADE_FARM_FEE_SPLIT_ADDRESSES,
  BLADE_VAULT_ADDRESSES,
  BLADE_FARMING_HELPER_ADDRESSES,
  BLADE_TRANSFER_HELPER_ADDRESSES,
} from '../../constants/blade'

# Blade

**Blade** is Sushi's RFQ (Request for Quote) trading protocol that provides better pricing through off-chain liquidity aggregation and on-chain settlement.

## Deployments

### Pool Register
<AddressTable data={BLADE_REGISTER_ADDRESSES} />

### Pool
<AddressTable data={BLADE_POOL_ADDRESSES} />

### Cove
<AddressTable data={BLADE_COVE_ADDRESSES} />

### Permit Router
<AddressTable data={BLADE_PERMIT_ROUTER_ADDRESSES} />

### LP Transfer
<AddressTable data={BLADE_LP_TRANSFER_ADDRESSES} />

### Fee Split
<AddressTable data={BLADE_FEE_SPLIT_ADDRESSES} />

### Farm Fee Split
<AddressTable data={BLADE_FARM_FEE_SPLIT_ADDRESSES} />

### Vault
<AddressTable data={BLADE_VAULT_ADDRESSES} />

### Farming Helper
<AddressTable data={BLADE_FARMING_HELPER_ADDRESSES} />

### Transfer Helper
<AddressTable data={BLADE_TRANSFER_HELPER_ADDRESSES} />
