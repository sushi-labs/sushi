# Reference: `sushi/stellar` Types

The Stellar types module exposes Stellar-specific type helpers.

| Type | Purpose |
|---|---|
| `StellarID` | ID type for Stellar currencies. |

Currency IDs follow the shared Sushi ID model and include the chain ID and currency identifier.
