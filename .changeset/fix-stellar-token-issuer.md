---
"sushi": patch
---

Type `StellarToken` issuer as `StellarAccountAddress` instead of `StellarAddress`. Stellar Classic asset issuers are always Ed25519 accounts (G-prefix), never Soroban contracts.
